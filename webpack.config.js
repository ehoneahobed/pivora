/**
 * Pivora uses native wp-scripts block discovery.
 * Build via: npm run build
 *
 * @see package.json scripts.build --source-path and --output-path
 */

module.exports = require( '@wordpress/scripts/config/webpack.config' );
